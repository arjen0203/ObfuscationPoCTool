code_path: C:\Users\arjen\Documents\Repos\ObfuscationPoCTool\obfuscation-poc-tool\TestFiles\TestCodes\Manual\quick_sort.c
output_path: C:\Users\arjen\Documents\Repos\ObfuscationPoCTool\obfuscation-poc-tool\TestFiles\TestOutput\Manual\quick_sort_low.c

techniques:
  - replacing_library_calls: {
        targetingType: identifier,
        targets: ["stdio.h"],
        replacementValue: "{*} -> {standardio.h}"
    }

  - replacing_library_calls: {
        targetingType: identifier,
        targets: ["stdlib.h"],
        replacementValue: "{*} -> {utility.h}"
    }

  - removing_comments: {
        targetingType: all
    }

  - replacing_identifiers: {
        targetingType: identifier,
        targets: ["printf"],
        replacementValue: "{*} -> {print}"
    }

  - replacing_identifiers: {
        targetingType: identifier,
        targets: ["display"],
        replacementValue: "{*} -> {show}"
    }

  - replacing_identifiers: {
        targetingType: identifier,
        targets: ["swap"],
        replacementValue: "{*} -> {switch}"
    }

  - replacing_identifiers: {
        targetingType: identifier,
        targets: ["first"],
        replacementValue: "{*} -> {f}"
    }

  - replacing_identifiers: {
        targetingType: identifier,
        targets: ["second"],
        replacementValue: "{*} -> {s}"
    }

  - replacing_identifiers: {
        targetingType: identifier,
        targets: ["partition"],
        replacementValue: "{*} -> {part}"
    }

  - replacing_identifiers: {
        targetingType: identifier,
        targets: ["pivot"],
        replacementValue: "{*} -> {point}"
    }

  - replacing_identifiers: {
        targetingType: identifier,
        targets: ["upper"],
        replacementValue: "{*} -> {up}"
    }
    
  - replacing_identifiers: {
        targetingType: identifier,
        targets: ["lower"],
        replacementValue: "{*} -> {low}"
    }

  - replacing_identifiers: {
        targetingType: identifier,
        targets: ["partitionIndex"],
        replacementValue: "{*} -> {index}"
    }

  - replacing_identifiers: {
        targetingType: identifier,
        targets: ["quickSort"],
        replacementValue: "{*} -> {algorithm}"
    }

  - replacing_identifiers: {
        targetingType: identifier,
        targets: ["scanf"],
        replacementValue: "{*} -> {scan}"
    }