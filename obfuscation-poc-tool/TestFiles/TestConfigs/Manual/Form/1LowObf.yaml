code_path: C:\Users\arjen\Documents\Repos\ObfuscationPoCTool\obfuscation-poc-tool\TestFiles\TestCodes\Manual\linear_search.c
output_path: C:\Users\arjen\Documents\Repos\ObfuscationPoCTool\obfuscation-poc-tool\TestFiles\TestOutput\Manual\linear_search_low.c

techniques:
  - replacing_library_calls: {
        targetingType: identifier,
        targets: ["stdio.h"],
        replacementValue: "{*} -> {standardio.h}"
    }

  - replacing_library_calls: {
        targetingType: identifier,
        targets: ["stdlib.h"],
        replacementValue: "{*} -> {utility.h}"
    }

  - removing_comments: {
        targetingType: all
    }

  - replacing_identifiers: {
        targetingType: identifier,
        targets: ["printf"],
        replacementValue: "{*} -> {print}"
    }

  - replacing_identifiers: {
        targetingType: identifier,
        targets: ["linearsearch"],
        replacementValue: "{*} -> {algorithm}"
    }

  - replacing_identifiers: {
        targetingType: identifier,
        targets: ["scanf"],
        replacementValue: "{*} -> {scan}"
    }