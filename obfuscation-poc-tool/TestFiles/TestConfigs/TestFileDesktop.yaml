code_path: C:\Users\arjen\Documents\Repos\ObfuscationPoCTool\obfuscation-poc-tool\TestFiles\TestCodes\Small.c
output_path: C:\Users\arjen\Documents\Repos\ObfuscationPoCTool\obfuscation-poc-tool\TestFiles\TestOutput\SmallOutput.c

techniques:
  - replacing_identifiers: {
        targetingType: identifier,
        targets: "amount",
        replacementValue: "{*} -> {newAmount}"
    }

  - abstracting_identifiers: {
        targetingType: identifier,
        targets: "secondAmount"
    }

  - replacing_static_values: {
        targetingType: identifier,
        targets: ["amount", "secondAmount"],
        replacementValue: "{*} -> {33}"
    }

  - replacing_static_values: {
        targetingType: identifier,
        targets: ["printText", "secondText"],
        replacementValue: "{test} -> {newTest}"
    }

  - replacing_static_values: {
        targetingType: identifier,
        targets: "single",
        replacementValue: "{*} -> {z}"
    }

  - abstracting_library_calls: {
        targetingType: identifier,
        targets: ["stdlib.h", "stdio.h"]
    }

  - abstracting_library_calls: {
        targetingType: all
    }

  - removing_comments: {
        targetingType: all
    }