code_path: C:\Users\arjen\Documents\Repos\ObfuscationPoCTool\obfuscation-poc-tool\TestFiles\TestCodes\Manual\linear_search.c
output_path: C:\Users\arjen\Documents\Repos\ObfuscationPoCTool\obfuscation-poc-tool\TestFiles\TestOutput\Manual\linear_search_med.c

techniques:
  - abstracting_library_calls: {
        targetingType: all
    }

  - removing_comments: {
        targetingType: all
    }

  - abstracting_identifiers: {
        targetingType: all
    }

  - replacing_lines_of_code: {
      targetingType: linesOfCode,
      targets: 18,
      replacementValue: "{Enter the size of the array:} -> {Size:}"
    }

  - replacing_lines_of_code: {
      targetingType: linesOfCode,
      targets: 22,
      replacementValue: "{Enter the contents for an array of size} -> {contents:}"
    }

  - replacing_lines_of_code: {
      targetingType: linesOfCode,
      targets: 27,
      replacementValue: "{Enter the value:} -> {Value:}"
    }

  - replacing_lines_of_code: {
      targetingType: linesOfCode,
      targets: 30,
      replacementValue: "{is in the array} -> {is in}"
    }

  - replacing_lines_of_code: {
      targetingType: linesOfCode,
      targets: 32,
      replacementValue: "{is not in the array} -> {is not in}"
    }