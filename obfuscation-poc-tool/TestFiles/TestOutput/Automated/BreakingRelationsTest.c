// Copyright example header
#include <stdio.h>
#include <string.h>

const char TEXT[] = "ABC";
int amount = 10;
void  a(int  startValue) {
        int calculatedAmount = (startValue + 6) / 2;return RepeatAndJoin(calculatedAmount, TEXT);
    }
int  b() {
        c(amount);return 0;
    }